{
  "name": "hyip",
  "private": true,
  "scripts": {
    "start": "meteor run --settings settings.json",
    "deploy": "meteor deploy www.financex.trade --settings settings.json"
  },
  "dependencies": {
    "babel-runtime": "^6.20.0",
    "meteor-node-stubs": "~0.2.4"
  }
}
